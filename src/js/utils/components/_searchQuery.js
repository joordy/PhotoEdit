export const searchQuery = (writtenQuery) => {
  console.log(writtenQuery)
  // console.log(writtenQuery)

  // const header = document.querySelector('#searchHeader')
  // const searchQuery = document.createElement('p')
  // const queryText = document.createTextNode(`Zoekterm: ${writtenQuery}`)

  // searchQuery.setAttribute('id', 'searchText')

  // header.appendChild(searchQuery)
  // // searchQuery.remove()
  // searchQuery.appendChild(queryText)
  // // String.trim(document.querySelector('#searchtext').textContent) //"hello"

  // // return header
  // let testje = document.querySelector('#searchText').textContent
  // if (testje === null) {
  //   console.log('error')
  // } else {
  //   console.log(testje)
  // }
}
